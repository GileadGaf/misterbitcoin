import { SinglePlurPipe } from './single-plur.pipe';

describe('SinglePlurPipe', () => {
  it('create an instance', () => {
    const pipe = new SinglePlurPipe();
    expect(pipe).toBeTruthy();
  });
});
